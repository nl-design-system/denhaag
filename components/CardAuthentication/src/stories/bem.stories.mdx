import { Canvas, Description, Meta, Story } from "@storybook/addon-docs";

import { argTypes, AuthenticationCard, defaultArgs, exampleArgs } from "../story-template";
import "../index.scss";
import pkg from "../../package.json";
import readme from "../../README.md";

<Meta
  title="CSS/Cards/Authentication Card"
  argTypes={argTypes}
  args={defaultArgs}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Description markdown={readme} />

## Stories

### Default

<Canvas>
  <Story name="Default" args={exampleArgs}>
    {AuthenticationCard.bind({})}
  </Story>
</Canvas>

### DigiD

<Canvas>
  <Story name="DigiD" args={{ ...exampleArgs, authProvider: "DigiD" }}>
    {AuthenticationCard.bind({})}
  </Story>
</Canvas>

### eHerkenning

<Canvas>
  <Story name="eHerkenning" args={{ ...exampleArgs, authProvider: "eHerkenning" }}>
    {AuthenticationCard.bind({})}
  </Story>
</Canvas>

### eIDAS

<Canvas>
  <Story name="eIDAS" args={{ ...exampleArgs, authProvider: "eIDAS" }}>
    {AuthenticationCard.bind({})}
  </Story>
</Canvas>

### Portrait

<Canvas>
  <Story name="Portrait" args={{ ...exampleArgs, portrait: true }}>
    {AuthenticationCard.bind({})}
  </Story>
</Canvas>

### Portrait with logo

<Canvas>
  <Story name="Portrait with logo" args={{ ...exampleArgs, authProvider: "DigiD", portrait: true }}>
    {AuthenticationCard.bind({})}
  </Story>
</Canvas>

### Alternative headingLevel

<Canvas>
  <Story name="Alternative Headinglevel" args={{ ...exampleArgs, headingLevel: 2 }}>
    {AuthenticationCard.bind({})}
  </Story>
</Canvas>
