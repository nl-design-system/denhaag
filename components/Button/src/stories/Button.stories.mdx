import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { DesignTokenDocBlock } from "storybook-design-token/dist/doc-blocks";
import { ArrowLeftIcon, ArrowRightIcon } from "@gemeente-denhaag/icons";

import { Button } from "../index";
import pkg from "../../package.json";
import readme from "../../README.md";

<Meta
  title="Components/Input/Button"
  component={Button}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "ALPHA",
    },
  }}
/>

<Description markdown={readme} />

## Design Tokens

<DesignTokenDocBlock categoryName="Button" viewType="table" />

## Stories

### Default

<Canvas>
  <Story
    name="Default"
    args={{}}
    parameters={{
      docs: {
        source: {
          code: `
import { Button } from "@gemeente-denhaag/button";
const ReactButton = <Button onclick={() => alert('Hello from Button 👋')}>Button</Button>;
// This 👆 transpiles into this 👇 in the DOM
<button class="denhaag-button denhaag-button--primary-action denhaag-button--medium">
  <span class="denhaag-button__label">Button</span>
</button>
    `,
        },
      },
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

<ArgsTable of={Button} />

### Primary Action

<Canvas>
  <Story
    name="Primary Action"
    args={{
      variant: "primary-action",
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

### Secondary Action

<Canvas>
  <Story
    name="Secondary Action"
    args={{
      variant: "secondary-action",
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

### Icon Left

<Canvas>
  <Story
    name="Icon Left"
    args={{
      startIcon: <ArrowLeftIcon />,
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

### Icon Right

<Canvas>
  <Story
    name="Icon Right"
    args={{
      variant: "secondary-action",
      endIcon: <ArrowRightIcon />,
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

### Medium

<Canvas>
  <Story
    name="Medium"
    args={{
      size: "medium",
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

### Large

<Canvas>
  <Story
    name="Large"
    args={{
      size: "large",
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    name="Disabled"
    args={{
      disabled: true,
    }}
  >
    {(args) => <Button {...args}>Button</Button>}
  </Story>
</Canvas>

### CSS/HTML component

<Canvas>
  <Story
    name="CSS/HTML component"
  >
    {(args) => <div>
      <button class={"denhaag-button denhaag-button--primary-action denhaag-button--medium"}>Button</button>
    </div>}
  </Story>
</Canvas>
