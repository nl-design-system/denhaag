import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { TextArea } from "../index";
import pkg from "../../package.json";
import readme from "../../README.md";
import { FormField, FormFieldLabel, FormFieldInput, FormFieldDescription } from "@gemeente-denhaag/form-field";

<Meta
  title="React/Input/Text Area"
  component={TextArea}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Description markdown={readme} />

## Stories

### Default

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
    }}
    name="Default"
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
      disabled: true,
    }}
    name="Disabled"
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

### Invalid input

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
      invalid: true,
    }}
    name="Invalid"
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

### With label

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
    }}
    name="With label"
  >
    {(args) => (
      <FormField>
        <FormFieldLabel htmlFor="textarea-1">Label</FormFieldLabel>
        <FormFieldInput>
          <TextArea id="textarea-1" aria-describedby="description-1" {...args} />
        </FormFieldInput>
        <FormFieldDescription id="description-1">
          A description providing more information about the input.
        </FormFieldDescription>
      </FormField>
    )}
  </Story>
</Canvas>

### Invalid with label

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
      invalid: true,
    }}
    name="Invalid with label"
  >
    {(args) => (
      <FormField error={true}>
        <FormFieldLabel htmlFor="textarea-1">Label</FormFieldLabel>
        <FormFieldInput>
          <TextArea id="textarea-1" aria-describedby="description-1" {...args} />
        </FormFieldInput>
        <FormFieldDescription id="description-1">
          A description providing more information about the input.
        </FormFieldDescription>
      </FormField>
    )}
  </Story>
</Canvas>

<ArgsTable of={TextArea} />
