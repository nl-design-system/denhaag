import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { TextArea } from "../index";
import pkg from "../../package.json";
import readme from "../../README.md";
import FormField from "@gemeente-denhaag/form-field";

<Meta
  title="React/Input/Text Area"
  component={TextArea}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Description markdown={readme} />

## Design Tokens

Not yet documented. Please refer to the source code.

## Stories

### Default

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
    }}
    name="Default"
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
      disabled: true,
    }}
    name="Disabled"
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

### Invalid input

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
      invalid: true,
    }}
    name="Invalid"
  >
    {(args) => <TextArea {...args} />}
  </Story>
</Canvas>

### With label

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
    }}
    name="With label"
  >
    {(args) => (
      <FormField
        input={<TextArea {...args} />}
        label={"Label"}
        description={"A description providing more information about the input."}
      />
    )}
  </Story>
</Canvas>

### Invalid with label

<Canvas>
  <Story
    args={{
      placeholder: "Placeholder",
      invalid: true,
    }}
    name="Invalid with label"
  >
    {(args) => (
      <FormField
        error={args.invalid}
        input={<TextArea {...args} />}
        label={"Label"}
        description={"A description providing more information about the input."}
      />
    )}
  </Story>
</Canvas>

<ArgsTable of={TextArea} />
