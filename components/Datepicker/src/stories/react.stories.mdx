import { Canvas, Description, Meta, Story } from "@storybook/addon-docs";

import { Datepicker } from "../index";
import { enUS } from "../locale";
import pkg from "../../package.json";
import readme from "../../README.md";

<Meta
  title="React/Input/Datepicker"
  component={Datepicker}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Description markdown={readme} />

## Stories

### Default

<Canvas>
  <Story
    name="Default"
    height="400px"
    args={{}}
    parameters={{
      docs: {
        source: {
          code: `
<Datepicker></Datepicker>
`,
        },
      },
    }}
  >
    {() => <Datepicker></Datepicker>}
  </Story>
</Canvas>

### With locale

<Canvas>
  <Story
    name="Locale"
    height="400px"
    args={{}}
    parameters={{
      docs: {
        source: {
          code: `
import Datepicker from "@gemeente-denhaag/datepicker";
import {enUS} from "@gemeente-denhaag/datepicker/locale";
<Datepicker locale={enUS}></Datepicker>
`,
        },
      },
    }}
  >
    {() => <Datepicker locale={enUS}></Datepicker>}
  </Story>
</Canvas>

### Error

<Canvas>
  <Story
    name="Error"
    height="400px"
    args={{}}
    parameters={{
      docs: {
        source: {
          code: `
<Datepicker error={true}></Datepicker>
`,
        },
      },
    }}
  >
    {() => <Datepicker error={true}></Datepicker>}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story
    name="Disabled"
    height="400px"
    args={{}}
    parameters={{
      docs: {
        source: {
          code: `
<Datepicker disabled={true}></Datepicker>
`,
        },
      },
    }}
  >
    {() => <Datepicker disabled={true}></Datepicker>}
  </Story>
</Canvas>
