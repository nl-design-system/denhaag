import { Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { DesignTokenDocBlock } from "storybook-design-token/dist/doc-blocks";

import { Datepicker } from "../index";
import { enUS } from "../locale";
import pkg from "../../package.json";
import readme from "../../README.md";

<Meta
  title="React Components/Input/Datepicker"
  component={Datepicker}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Description markdown={readme} />

## Design Tokens

<DesignTokenDocBlock categoryName="Datepicker" viewType="table" />

## Stories

### Default

<Canvas>
  <Story
    name="Default"
    args={{}}
    parameters={{
      docs: {
        source: {
          code: `
<Datepicker></Datepicker>;
`,
        },
      },
    }}
  >
    {() => <Datepicker></Datepicker>}
  </Story>
</Canvas>

### With locale

<Canvas>
  <Story
    name="Locale"
    args={{}}
    parameters={{
      docs: {
        source: {
          code: `
import Datepicker from "@gemeente-denhaag/datepicker";
import {enUS} from "@gemeente-denhaag/datepicker/locale";
<Datepicker locale={enUS}></Datepicker>;
`,
        },
      },
    }}
  >
    {() => <Datepicker locale={enUS}></Datepicker>}
  </Story>
</Canvas>
