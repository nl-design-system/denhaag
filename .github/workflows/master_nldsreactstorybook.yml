# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Storybook app to Azure Web App - nldsreactstorybook

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master

    - name: Set up Node.js version
      uses: actions/setup-node@v1
      with:
        node-version: '14.x'

    - name: yarn install, build storybook
      run: |
        npm install -g yarn
        yarn install
        yarn build-storybook
		
    - name: 'Deploy to Azure Web App'
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'nldsreactstorybook'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_0d5d1a2505b24878aee2e524b99ca2c8 }}
        package: storybook-static
