import { Canvas, Meta, Story } from "@storybook/blocks";
import { IconButton, TrashIcon } from "@gemeente-denhaag/icons";

import pkg from "../../../../components/IconButton/package.json";

<Meta
  title="React/Input/Icon button"
  component={IconButton}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "ALPHA",
    },
  }}
/>

## Stories

### Default

<Canvas>
  <Story
    name="Default"
    args={{
      "aria-label": "delete",
    }}
  >
    {(args) => (
      <IconButton {...args}>
        <TrashIcon />
      </IconButton>
    )}
  </Story>
</Canvas>

## Accessibility

Make sure to add an `aria-label: "<description of button>"` to the attributes of `IconButton` for screen-readers.
