import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/blocks";
import { action } from "@storybook/addon-actions";

import { HouseIcon, TrashIcon } from "@gemeente-denhaag/icons";
import { List, ListItem, ListSubheader } from "@gemeente-denhaag/list";

import pkg from "../../../../components/List/package.json";
import readme from "../../../../components/List/README.md";

<Meta
  title="React/Data Display/List"
  component={List}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Description markdown={readme} />

## Usage

This component can be used with multiple (optional) properties for the ListItem:

- Primary text
- Secondary text
- Type: nav or action
- Left icon
- Secondary Action Icon

## Stories

### Default

<Canvas>
  <Story
    name="Default"
    args={{ subheader: <ListSubheader>Subheader</ListSubheader> }}
    parameters={{
      docs: {
        source: {
          code: `
import { List, ListSubheader, ListItem } from "@gemeente-denhaag/list";
<List subheader={<ListSubheader>Subheader</ListSubheader>}>
  <ListItem primaryText="ListItem" />
  <ListItem primaryText="ListItem" />
  <ListItem primaryText="ListItem" />
</List>
          `,
        },
      },
    }}
  >
    {(args) => (
      <List {...args}>
        <ListItem primaryText="ListItem" />
        <ListItem primaryText="ListItem" />
        <ListItem primaryText="ListItem" />
      </List>
    )}
  </Story>
</Canvas>

<ArgsTable of={List} />

### All ListItem types

<Canvas>
  <Story
    name="All ListItem types"
    args={{ subheader: <ListSubheader>Subheader</ListSubheader> }}
    parameters={{
      docs: {
        source: {
          code: `
import { List, ListSubheader, ListItem } from "@gemeente-denhaag/list";
import { HouseIcon, TrashIcon } from "@gemeente-denhaag/icons";
<List subheader={<ListSubheader>Subheader</ListSubheader>}>
  <ListItem primaryText="ListItem" />
  <ListItem
    primaryText="Single-line item"
    actionType="action"
    leftIcon={<HouseIcon color="inherit"></HouseIcon>}
    rightIcon={<TrashIcon />}
    onClick={()=> {alert('Secondary Action Click')}}
  />
  <ListItem
    primaryText="Single-line item"
    secondaryText="Secondary text"
    leftIcon={<HouseIcon color="inherit" />}
    actionType="nav"
    onClick={()=> {alert('Nav Click')}}
  />
</List>
          `,
        },
      },
    }}
  >
    {(args) => (
      <List {...args}>
        <ListItem primaryText="ListItem" />
        <ListItem
          primaryText="Single-line item"
          actionType="action"
          leftIcon={<HouseIcon color="inherit"></HouseIcon>}
          rightIcon={<TrashIcon />}
          onClick={action("Secondary action click")}
        />
        <ListItem
          primaryText="Single-line item"
          secondaryText="Secondary text"
          leftIcon={<HouseIcon color="inherit" />}
          actionType="nav"
          onClick={action("ListItem Nav click")}
        />
      </List>
    )}
  </Story>
</Canvas>
