import { Canvas, Meta, Story, ArgsTable } from "@storybook/blocks";
import { StepMarker } from "../../../../components/StepMarker/src/index";
import pkg from "../../../../components/StepMarker/package.json";
import "../index.scss";
import { AlertTriangleIcon, CheckedIcon, CloseIcon } from "@gemeente-denhaag/icons";

export const Template = (args) => <StepMarker {...args} />;

<Meta
  title="React/Process Steps/StepMarker"
  component={StepMarker}
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: {
      source: { type: "dynamic" },
    },
    status: {
      type: "ALPHA",
    },
  }}
  argTypes={{
    children: {
      control: "select",
      options: [1, '"A"', "<CheckedIcon/>", "<AlertTriangleIcon/>", "<CloseIcon/>"],
      mapping: {
        "1": <>1</>,
        '"A"': <>A</>,
        "<CheckedIcon/>": <CheckedIcon />,
        "<AlertTriangleIcon/>": <AlertTriangleIcon style={{ marginBlockStart: "-2px" }} />,
        "<CloseIcon/>": <CloseIcon />,
      },
    },
  }}
/>

# Stories

## Default

<Canvas>
  <Story name="Default" args={{ children: 1, appearance: "not-checked", nested: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Not Checked

<Canvas>
  <Story name="Not Checked" args={{ appearance: "not-checked", children: "1" }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Not Checked Nested

<Canvas>
  <Story name="Not Checked Nested" args={{ appearance: "not-checked", nested: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Checked

<Canvas>
  <Story name="Checked" args={{ appearance: "checked", children: <CheckedIcon /> }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Checked Nested

<Canvas>
  <Story name="Checked Nested" args={{ appearance: "checked", children: <CheckedIcon />, nested: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Current

<Canvas>
  <Story name="Current" args={{ appearance: "current", children: "1" }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Current Nested

<Canvas>
  <Story name="Current Nested" args={{ appearance: "current", nested: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled

<Canvas>
  <Story name="Disabled" args={{ appearance: "disabled", children: "1" }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Disabled Nested

<Canvas>
  <Story name="Disabled Nested" args={{ appearance: "disabled", nested: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Warning

<Canvas>
  <Story name="Warning" args={{ appearance: "warning", children: <AlertTriangleIcon style={{ marginTop: "-2px" }} /> }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Warning Nested

<Canvas>
  <Story name="Warning Nested" args={{ appearance: "warning", nested: true }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Error

<Canvas>
  <Story name="Error" args={{ appearance: "error", children: <CloseIcon /> }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Error Nested

<Canvas>
  <Story name="Error Nested" args={{ appearance: "error", children: <CloseIcon />, nested: true }}>
    {Template.bind({})}
  </Story>
</Canvas>
