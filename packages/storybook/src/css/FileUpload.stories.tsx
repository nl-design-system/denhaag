import reactMeta from '../react/FileUpload.stories';

const meta: typeof reactMeta = {
  ...reactMeta,
  tags: ['autodocs'],
};

export { Default } from '../react/FileUpload.stories';

export default meta;
