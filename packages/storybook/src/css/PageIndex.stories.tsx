import reactMeta from '../react/PageIndex.stories';

const meta: typeof reactMeta = {
  ...reactMeta,
  tags: ['autodocs'],
};

export default meta;

export { Default } from '../react/PageIndex.stories';
