import reactMeta from '../react/RichText.stories';

const meta: typeof reactMeta = {
  ...reactMeta,
  tags: ['autodocs'],
};

export default meta;

export { Default } from '../react/RichText.stories';
