import reactMeta from '../react/Link.stories';

const meta: typeof reactMeta = {
  ...reactMeta,
  title: 'CSS/Link',
  id: 'css-link',
  tags: ['autodocs'],
};

export { Default, Disabled, IconStart, IconEnd } from '../react/Link.stories';

export default meta;
