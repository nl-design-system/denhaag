import { Canvas, Description, Meta, Story } from "@storybook/blocks";
import "../index.scss";
import pkg from "../../package.json";
import readme from "../../README.md";
import { AuthenticationCardGroup } from "../story-template";

<Meta
  title="CSS/Cards/Card Group"
  parameters={{
    componentSubtitle: `${pkg.name} - ${pkg.version}`,
    docs: { source: { type: "dynamic" } },
    status: {
      type: "WORK IN PROGRESS",
    },
  }}
/>

<Description markdown={readme} />

## Stories

### Authentication Card Group Default

<Canvas>
  <Story name="Authentication Card Group" args={{ cards: ["DigiD", "eHerkenning", ""] }}>
    {AuthenticationCardGroup.bind({})}
  </Story>
</Canvas>

### Authentication Card Group Rows

<Canvas>
  <Story name="Authentication Card Group Rows" args={{ cards: ["DigiD", "eHerkenning", ""], rows: true }}>
    {AuthenticationCardGroup.bind({})}
  </Story>
</Canvas>

### Responsive Authentication Card Group

<Canvas>
  <Story
    name="Responsive Authentication Card Group"
    args={{ cards: ["DigiD", "eHerkenning", "eIDAS", ""], responsive: true }}
  >
    {AuthenticationCardGroup.bind({})}
  </Story>
</Canvas>
