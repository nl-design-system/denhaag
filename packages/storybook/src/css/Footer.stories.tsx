import reactMeta from '../react/Footer.stories';

const meta: typeof reactMeta = {
  ...reactMeta,
  tags: ['autodocs'],
};

export default meta;

export { Default, TopOnly, BottomOnly } from '../react/Footer.stories';
