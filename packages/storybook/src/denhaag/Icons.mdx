import { IconGallery, IconItem } from "@storybook/addon-docs";
import * as Icons from "../../../../components/Icons/";

# Icons

The icons are available as SVG image and as React component.

<IconGallery>
  {Object.entries(Icons)
    .filter(([name]) => name !== "default")
    .map(([name, Icon]) => (
      <IconItem key={name} name={name.replace(/^(.+)Icon$/, "$1")}>
        {<Icon />}
      </IconItem>
    ))}
</IconGallery>

## Design Kit

[Figma: Brand Identity / Icon](https://figma.com/file/JpoY3waVoQGlLQzQXTL9nn/Design-System-Gemeente-Den-Haag?node-id=3497%3A14242)
