import { ArgsTable, Canvas, Description, Meta, Story } from "@storybook/addon-docs";
import { DesignTokenDocBlock } from "storybook-design-token/dist/doc-blocks";

import { MenuButton, MenuButtonProps } from '.';
import pkg from '../package.json';
import readme from "../README.md";

<Meta
  title="Components/Navigation/MenuButton"
  component={MenuButton}
  parameters={{ componentSubtitle: `${pkg.name} - ${pkg.version}`, docs: {source: {type: "dynamic"} } }}
/>

<Description markdown={readme} />

## Design Tokens

<DesignTokenDocBlock categoryName="MenuButton" viewType="table" />

## Stories

### Default

<Canvas>
  <Story name="Default"
    args={{href: 'https://github.com/nl-design-system/denhaag', variant: 'normal'}}
    parameters={{
      docs: {
        source: {
          code: `
          import { MenuButton } from "@gemeente-denhaag/menubutton";
          const ReactMenuButton = <MenuButton href={https://github.com/nl-design-system/denhaag} variant='normal'>Label</MenuButton>};
          // This 👆 transpiles into this 👇 in the DOM
          <a href="https://github.com/nl-design-system/denhaag" class="denhaag-menu-button">
            Label
          </a>`,
        },
      },
    }}>
      {(args) => <MenuButton { ...args }>Label</MenuButton>}
  </Story>
</Canvas>

<ArgsTable of={MenuButton} />

### Expandable

<Canvas>
  <Story name="Expandable"
    args={{variant: 'expandable'}}
    parameters={{
      docs: {
        source: {
          code: `
          import { MenuButton } from "@gemeente-denhaag/menubutton";
          const ReactMenuButton = <MenuButton variant='expandable' onclick={() => alert('Hello from MenuButton 👋')>Label</MenuButton>};
          // This 👆 transpiles into this 👇 in the DOM
          <button class="denhaag-menu-button">
            Label
            <span class="denhaag-menu-button__chevron">
              <svg width="24" height="24" fill="none" xmlns="http://www.w3.org/2000/svg" class="MuiSvgIcon-root" focusable="false" viewBox="0 0 24 24" aria-hidden="true" shape-rendering="auto" aria-label="ChevronDownIcon">
                <path d="M5.293 8.293a1 1 0 011.414 0L12 13.586l5.293-5.293a1 1 0 111.414 1.414l-6 6a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414z" fill="currentColor"></path>
              </svg>
            </span>
          </button>`,
        },
      },
    }}>
      {(args) => <MenuButton { ... args} onclick={() => alert('Hello from MenuButton 👋')}>Label</MenuButton>}
  </Story>
</Canvas>

### Menu
<Canvas>
  <Story name="Menu"
    parameters={{
      docs: {
        source: {
          code: `
          `,

         }
      }
    }}>
      {(args)} => <Menu>
                    <MenuButton>
                      Test
                    </MenuButton>
                    <MenuButton>
                      Test2
                    </MenuButton>
                  </Menu>
  </Story>
</Canvas>
